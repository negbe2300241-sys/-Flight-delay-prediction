# -Flight-delay-prediction
Nigeria-focused Flight Delay Prediction system. Uses a Random Forest model trained on synthetic Nigerian airline data and provides real-time predictions via a Flask web interface.
